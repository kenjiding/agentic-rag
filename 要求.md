
## 需求要求
  - 请深入访谈我对需求和更改的想法和意图, 当你真的get到我的点后再开始编码和修改

## 代码要求
 - 必须首先完全理解代码逻辑和架构后, 然后输出修改步骤后(但需要认真审查新增的功能代码是否是冗余的,或者其他功能已经存在了相关的功能, 不要引入重复的, 冗余的功能代码), 再使用企业级最佳实践开始有条理的修改
 - 修改不能有补丁代码，不能有堆叠代码, 不要使用魔术代码, 包括不能使用魔术字符串, 不能使用hack代码临时解决问题
 - 对于prompt, 不要只针对某个问题做适配,硬代码, 而是深入思考写出深度适配通用场景的prompt
 - **不需要写兼容代码, 不需要写向后兼容代码**
 - 不能只针对某个问题做兼容修复, 要通用的实现方案
 - 必须是要从源头找到根源问题, 再从根源到发生问题的链路上解决问题
 - 不要写重复的代码,代码不要都聚集在一个方法内, 需要一开始就设计好架构,然后按照功能去抽象和封装和收敛
 - 对于中断,恢复机制的需求请使用langgraph 1.x的 interrupt和Command实现


## 对于多步骤任务,正确的流程如下
 1. 用户提问“帮我购买3个西门子产品, 我的电话是13566667676”
 2. ai首先经过products_agent搜索产品, 然后返回可以让用户选择的商品列表
 3. 用户选中了某个商品
 4. ai进行信息确认, 然后出现确认订单的交互ui
 5. 用户点击"确认"订单, ai进行真正的下单操作. 点击"取消"ai取消购买下单流程
 6. 点击"取消"下单, ai需要恢复正常的客服沟通流程, 清理多任务 or 确认等流程数据. 用户其他提问进行对应的回答

 ## 项目
  - 使用langgraph 1.x最新的官方实践 interrupt和Command实现中断,恢复功能
  - 因为现在项目都适用Pydantic了. 不需要兼容字段的操作, 只支持Pydantic的写法就行

## 介绍
  重构supervisor模式, 支持A2A协议, Group协作,自纠, 讨论

  通用Agent样样都会, 就是不精.
  垂直Agent只会某事, 但是也只会某事
  对于不训练模型的Agent都是逆向工程


我觉得这个format_state_update不是最佳实践，所有不同类型的format逻辑都在这里做转换，这样会带来很多的不同判断的代码，维护非常困难！
我觉得是不是在某个节点执行的时候就应该把对应的数据处理好，最后返回到stream里直接让前端展示就行。不要所有逻辑都经过format_state_update处理，而是从源头就已经处理好！！！
按照你对代码的熟悉程度我的理解是对的吗?



# 存在的问题

## 现在的问题
1.我觉得意图识别的llm调用的时候只分析了当前的最新一个用户提问, 以及提取这个最新的提问的entities。
 但是messages上下文还有很多tool message返回的信息没有利用到上下文中. 这样就导致了很多信息没有被利用到, 比如用户已经选定了产品, 但是意图识别还是认为用户没有选定产品, 导致还是会route到product agent去搜索产品拿到product ids, 然后再进行对比任务.

2.还有就是supervisor内的llm也会提前提取上下文信息, 比如@supervisor.py (512-516) , 这和意图识别的上下文提取是否重合,冗余？？

## 我的想法
 - 因为意图识别和route都会使用llm去分析内容和提取内容.
但是意图识别只专注当前的最新问题做识别是企业级最佳实践呢,还是需要根据上下文做意图识别才是企业级最佳实践呢?? 
 - 既然state messages已经包含了很多上下文，是不是需要充分利用这些上下文的内容，然后route或者意图识别才能更准确的决策。
 
## 困境
但是我不知道在那部分处理最好。所以需要你仔细的分析项目架构和逻辑，再分析、计划。

## 目的
在企业级最佳实践中,这个上下文信息压缩应该怎么处理最好? 在那部分逻辑实现才是最佳实践?

## 你的思考
你必须首先完全理解代码逻辑和架构后，再开始思考，分析。不能只针对当前的问题做场景问题的适配，要设计通用，智能的方案处理，而不是枚举。需要对整体的把持，全局的眼光审视， 可扩展等等。
